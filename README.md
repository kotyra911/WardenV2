# 🔍 Avito Parser with Telegram Notifications

Асинхронный парсер объявлений авито, который отслеживает новые объявления по заданной ссылке и отправляет уведомления в Telegram, исключая дубликаты с помощью Redis.

---

## 🚀 Features

- 🔗 Переход по заданной ссылке
- 📄 Сбор всех объявлений со страницы
- 🧠 Защита от дубликатов через Redis
- ⏳ TTL для объявлений — **2 дня**
- 📬 Уведомления о новых объявлениях в Telegram
- 🛠 Автоматический debug-режим с отправкой логов админу

---

## ⚙️ How It Works

1. Парсер получает ссылку на страницу с объявлениями
2. Переходит по ссылке и собирает все найденные объявления
3. Для каждого объявления:
   - Проверяет наличие в Redis
   - Если объявление **новое**:
     - Сохраняет его в Redis с TTL = 2 дня
     - Отправляет уведомление в Telegram
   - Если объявление **уже обрабатывалось** — пропускает его
4. При возникновении ошибки:
   - Лог автоматически отправляется администратору в Telegram

---

## 🧠 Redis Logic

Redis используется как локальное хранилище обработанных объявлений:

- **TTL** — 2 дня  

В базе хранится хеш, который получается из комбинированной строки.
Метод хеширования - **SHA256**

---

## 🧪 Debug & Error Handling

В проекте реализован механизм автоматической отладки:

- Лог-файл отправляется администратору в Telegram
- Позволяет быстро реагировать на проблемы без доступа к серверу

---

## 📦 Tech Stack

- **Python 3.12+**
- **Redis**
- **Docker**
- **Telegram Bot API**
- **AsyncIO**
- (полный список зависимостей см. `requirements.txt`)

---

## ! ВАЖНО !

  Если вы хотите, чтобы логи отправлялись вам в тг, то нужно сделать следующее:

- В bot/notifications.py раскомментировать функцию send_log_to_admin
- В bot/utils/logger.py раскомментировать "Ведение лог файла"
- В .env указать TG_ADMIN

  Можно также указать сколько угодно ID и отправлять объявление десятку людей.
  Главное дублировать метод отправки сообщения через TelegramAPI

---

## ▶️ Getting Started

```bash
    git clone https://github.com/kotyra911/WardenV2.git
    
    скачайте драйвер Chrome
    
    cd путь проекта
    
    создайте .env на основе env
    
    python -m venv venv  # создаение виртуального окружения
    
    source venv/bin/activate  # Linux / macOS  # активация venv
    
    venv\Scripts\Activate     # Windows  # активация venv
    
    pip install -r requirements.txt  # Установке

    запустить bot/main.py
